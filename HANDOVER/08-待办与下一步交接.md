# 08 · 待办与下一步交接

> 给下一个 Agent 的"从这里接着做"。分「刚完成」「遗留问题」「建议方向」三部分。

## 一、刚完成的（本交接最近一轮）

1. **仪式卡片按钮被挡修复**：`Game.jsx` 仪式卡片容器改 `pointer-events:none`，只在可交互子元素开 `pointer-events:auto` → 「卦已装成·校准用神」等右下角按钮不再被透明容器挡住（已浏览器实测点击成功推进 P3→P4）。
2. **P1-P3 栏与顶部流程栏重叠修复**：仪式卡片容器 `maxHeight:min(78vh,780px)` + 阶段卡片 `maxHeight:min(58vh,560px)` + `overflowY:auto` → 高内容不再上顶到顶部 ProcessStepper。
3. 历史已修复：仪式自动跳走、三变勾选框放大、删除 3D 棍状模型、两径按问题关键词生成、429 降级推荐、装卦日志渲染、底部导航收窄等。

## 二、遗留问题 / 需注意

- **三变勾选无实际作用**：勾选仅翻转 UI 状态，不卡流程、不进命牌/总结（见 [03](03-仪式-三变-两径说明.md)）。
- **两径/三忌三要是预设模板**，与用户具体措辞仅"关键词类别"相关，未结合智囊辩论结论 → 用户反馈"没东西、和问题没关系"。
- **生产验证**：最新 UI 修复尚未重新发布到 `yance-bagua.surge.sh`（本地 `:5173` 已实测）。下次交付需 `npm run build` + `npx surge dist yance-bagua.surge.sh`，并强刷验证 `/sandbox`。

## 三、建议的下一步（按价值排序）

### P0（直接提升用户感知）
1. **两径改 LLM 个性化生成**：`buildSanBian` 的两径/三忌三要改为可选地由 LLM 按「用户问题 + 卦象 + 智囊辩论摘要」生成，失败再回落当前模板兜底。这直接解决"和问题没关系"。
2. **三变勾选落地**：把勾选的忌/要写进命牌（禁忌/锦囊）与演总结，让"承诺"有意义（当前完全孤立）。

### P1（体验与稳定）
3. 复核 `useGameFlow.js` 超大文件：是否有仍会 `setPhase` 跳仪式阶段的隐患；必要时拆分。
4. 检查 `oracle`/`committing`/`branch_select` 等阶段与底部导航按钮的 z-index / pointer-events 是否同样安全。
5. 端到端自动化：写一条冒烟脚本（起念→投钱→装卦→用神→三变→辩论→命牌），防止回归。

### P2（架构）
6. `server/` 与 `worker/` 后端二选一收敛，避免双后端漂移。
7. 清理 `docs/archive`、`archive/`、`scripts/legacy` 的过时文档，统一以 `HANDOVER/` 为权威入口。
8. 评估是否真需要 Three.js 3D 推演台：用户对 3D 特效反感（多次要求删除），可考虑 2D 化降复杂度。

## 四、给下一个 Agent 的开工清单

1. 先读本 `HANDOVER/00`（总览）+ `07`（约束）。
2. `cd divergence-trae && npm run dev`，浏览器打开 `http://localhost:5173/sandbox`。
3. 走一遍核心流程，看当前状态。
4. 从「P0」两条开始做，做完 `npm run build` 验证 + 实测点击 + 发布 Surge + 强刷验证。
5. 遵循 [07](07-工程约束与踩坑记录.md) 的约束，尤其是"仪式阶段不自动推进"与"指针/z-index 挡按钮"。
