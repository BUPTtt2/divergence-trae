01 模型层

✅ 对：分档思路保留。

➕ 加：小模型输出必须过代码阈值才生效；超预算时「降档收敛」而不是报错。

➖ 删：任何「单一模型包打」的假设。

02 Agent 框架

✅ 对：状态机思路。

🔧 改：如果原文是自研状态机 → 换成 LangGraph，别造轮子。

➕ 加：任意节点可杀进程恢复的验收项。

03 协议层

➕ 加：事件流 schema 清单（stage.enter / question.ask / card.seal / echo.due…），前端只消费事件。

➖ 删：前端直调引擎的任何路径。

04 工具层

✅ 对：工具化方向。

🔧 改：全部包成 MCP，别用私有接口。

➕ 加：ask\_user 必须带 reason 且用户可见；q\_registry 拦重复提问；圆桌 Agent 默认无联网权。

05 记忆系统

✅ 对：分层思路。

🔧 改：底座用 Mem0，别自研存储。

➕ 加：新旧矛盾带时间戳共存，不覆盖；L2 传记 ≤2k token 永远加载。

➖ 删：覆盖式写入逻辑。

06 RAG 管线

➕ 加：证据管线四步 search→fetch→extract→cite，产出带来源的证据卡。

🔧 改：传记不走检索，常驻上下文。

07 上下文工程

➕ 加：context\_assembler 带 token 预算裁剪；圆桌实例强制带「已知信息不许重问」清单。

这条如果原文档没有，是整个项目最容易漏、最致命的一条。

08 结构化输出

✅ 对：用原生 JSON Schema。

➖ 删：自研解析器。

➕ 加：解析失败 → 重试 → fallback → 降级三档，不崩流程。

09 错误处理

➕ 加：单 Agent 失败不阻塞整桌（retry→fallback→跳过）；用户敌对/玩闹时的降级路径。

原文档如果只写了 retry，不够。

10 Human-in-the-Loop

✅ 对：interrupt 点思路。

➕ 加：「暂不选择」是一等结局；隔天回来有小模型 recap 续跑。

11 多 Agent 协作 ← 改动最大的一块

🔧 改：主持是代码，不是 LLM。A 轮并行、B 轮串行、C 轮至多一问，全部写死。

➕ 加：反驳必须钉到 claim\_store 的 claim\_id，无靶子的反驳直接拦截；过度一致时自动注入 devils\_advocate。

➖ 删：如果原文是「轮流发言」式圆桌，整个删掉重写。

12 Prompt 管理

🔧 改：任何 prompt 改动必须先 shadow run（新 prompt 并行重跑旧会话，判官对比），再 A/B，再上线。直接上线的路径删掉。

13 可观测性

✅ 对：Langfuse。

➕ 加：事件流可回放——抽任一 session 能还原完整决策路径。

14 评测体系

✅ 对：promptfoo 框架。

➕ 加：合成用户（LLM 按画像扮演用户）做回归；指标里必须有预测校准度和30 天后悔率——这两个是你的自创指标，市面框架没有。

15 后端服务

➕ 加：回响调度（7/30 天）独立任务队列；引擎可单独重启不影响已发分享链接。

16 前端

➕ 加：用录制事件流回放测试——断掉真实 LLM，前端演出必须完整无误。这条是验收硬门槛。

17 部署运维

➕ 加：换模型档位不重启服务的演练；checkpoint 备份恢复演练。

18 Agent 安全

✅ 对：护栏思路。

➕ 加：客席 spec、市集 Agent、附件这三类不可信输入必须过校验（是否预测命运/是否夹带指令）；卡片页脚常驻免责。

19 数据飞轮

➕ 加：客席之灵「频次+受用达标→转正常驻」的完整路径；bad case 自动入 eval 集。

原文档如果只写了「收集反馈」，不够，要有闭环。

20 成本控制

➕ 加：圆桌 B 轮串行（省钱纪律）；冷启动 demo 卦零 LLM 成本。

